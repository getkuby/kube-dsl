parser:
  # Don't exclude the `sorbet` directory, overriding the default
  excluded_paths: []

  # Look only in the kube-dsl types directory
  included_paths:
  - ./sorbet/rbi/kube-dsl
  - ./lib/kube-dsl/dsl_object.rb
  - ./lib/kube-dsl/resource.rb
  - ./lib/kube-dsl/value_fields.rb
